# 更新日志

记录每个版本对外可见的改动。**发版（打 tag 触发 Release）前，务必把本次改动补进这里。**

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [待发布]

### 修复（读屏：图片不再只是干巴巴一句「图片」）

- **用读屏软件读书时，图片有了说明文字**
  - 封面会念出书名（例如「《策略思维》封面」），正文插图念作「插图」；书里**自己写了**图注的，一律原样保留、不做改动。
  - 这条只影响读屏用户，屏幕上的样子没有任何变化。

### 修复（键盘：快捷键速查表滚不动）

- **打开「?」快捷键速查表后，可以用键盘滚动了**
  - 表比较长的时候，原先只有鼠标滚轮能够到它（键盘按不进那个区域），于是最下面的「触屏手势」那一组根本看不到。
  - 现在 Tab 能落到内容区、上下键可以滚动，聚焦框也看得见。

## [0.1.6] — 2026-09-16

### 修复（Esc 键：打开搜索/书签面板后按 Esc 会整本书退出去）

- **打开「搜索」或「书签」面板后按 Esc，现在只会关掉面板，不再一路退回书库**
  - 这个 bug 藏得比较深：按 Esc 的处理逻辑里，"当前有没有面板开着"读到的是**旧状态**。于是从「搜索 / 书签 / 笔记」面板按 Esc，程序以为"什么都没开"，直接执行了"返回书库"。
  - 目录与排版两个面板**恰好**读的是新状态，所以一直表现正常 —— 这也是它长期没被发现的原因；而按 Esc 想关掉搜索框的用户，会被莫名其妙地扔回书架（在哪儿读到半截都得重新翻）。
  - 同一个毛病还影响了**框选文字后弹出的高亮确认浮层**：按 Esc 本意是"我选错了，取消"，实际却把整本书关了。现在一并修正，并补了回归测试（改回去会当场变红）。

### 新增（快捷键：终于有入口了）

- **阅读页顶栏最右边多了个「?」，随时能查出所有快捷键**
  - 这些键其实一直都在（`Ctrl+B` 标记书签、`Ctrl+F` 搜索、`Ctrl+Home/End` 书首书尾、`Ctrl+PageUp/PageDown` 上一章下一章、方向键、空格、`Esc`），但界面上**一个字都没提过**——用户不知道有这些键，等于功能白做一半。
  - 现在点「?」（或直接按 `?` 键）就打开速查表，按「翻屏 / 章节与位置 / 面板与操作」分组列清楚；**手机端还能查到四条触屏手势**（点左 / 右边缘翻屏、左右滑动翻屏、点中间收起顶栏、长按选中文字加高亮）。
  - Mac 上显示的是 `⌘+B`，Windows / Linux 上显示 `Ctrl+B`；查完按 `Esc` 或再按一次 `?` 收起，焦点会回到那个「?」按钮上。
- **第一次进阅读页会有一次轻提示**
  - 右上角弹一句「键盘也能翻书」，点它就直接打开速查表；8 秒后自己消失，不耽误看书。
  - 不想再看到 → 点「不再提示」，以后永远不弹；只是这一会儿不想看 → 点「×」，本次会话内不再出现（重开标签页后会再提示一次）。

### 修复（手机端划词：三条真机反馈）

- **划完词要"再点一次"才弹浮层，现在一次就出来**
  - iPhone Safari、安卓夸克、荣耀浏览器实测都是这个表现，而且时灵时不灵（iPhone 尤其难）。根因是手机浏览器"选中文字"这件事**慢半拍**：手指离开屏幕那一瞬间选中的内容往往还没准备好，程序去问"选中了什么"，只能得到"什么都没选"。
  - 现在改成"等选中内容真正定下来再弹"，不再抢那一瞬间；同时保证**拖选区把手的过程中不会弹**——否则选区被清掉、手柄突然消失，选词选到一半就废了。
- **划在屏幕最底部时，浮层的「加高亮 / 取消」不再被屏幕吃掉**
  - 以前浮层只会往选区下方挂，下方不够就顶到屏幕外——划屏幕底部的字经常确认不了、也取消不了。
  - 现在下方放不下会自动翻到选区**上方**；上下都放不下（横屏等）则把浮层整体限制在屏幕内，按钮始终够得着。
- **不做选择时，翻页或滚动会自动收起浮层**
  - 浮层不跟着正文滚：一旦翻页它还悬在原地，既像"卡住了"，也容易手滑点到「加高亮」，把高亮加到已经翻走的那一段上。现在一滚动就收起（微信读书 / Kindle 是同样的做法）。
  - 例外：**笔记框里已经写了字时不收**。写一半被自己的滑动动作关掉，那才是真添乱。

### 修复（手机端：划词加高亮根本弹不出来）

- **手机网页版上，选中文字后不再"什么都不会发生"**
  - 高亮的确认浮层原先只挂在鼠标的「松开」事件上，而触屏压根不产生这个事件；代码一看"有选区"就直接收手不干。桌面版（dmg）用鼠标不受影响，但**手机这半数用户的核心功能等于没有**。
  - 现在手指离开屏幕时，只要还留着一段选中的文字，确认浮层就会弹出来（点「加高亮」才入库，点别处即取消，和桌面一致）。
  - 顺带解决两个连带坑：**拖选区把手不再顺手翻一屏**（拖把手本身就是横向移动，以前会被当成"划页"手势）；浮层改挂在选区**下方**，避开手机系统自带的「复制 / 共享」气泡，不再两套菜单叠在一起。
  - 触屏弹出的浮层**不再自动弹起软键盘**——键盘一冒出来就把浮层盖住了；要写笔记点一下输入框即可（桌面鼠标行为不变）。

### 修复（无障碍：读屏用户终于"听得到"这些提示）

- **导出结果、书签提示、「已回到上次阅读位置」都会被子读出**
  - 这三条以前是普通文本块，屏幕阅读器完全听不到——"导出成功了没""这段是不是已经高亮过"全成了静默操作。
  - 现在它们是标准的状态区（`role="status"` + `aria-live="polite"`）：不打断当前朗读、等一句念完再播报，与书库的「已移除 / 撤销」提示保持一致。
- **四个高亮色块上直接写出了它们的含义**
  - 高亮颜色本来就是「重点 / 疑问 / 待查 / 喜欢」四种语义，可界面上一直只是四个 26px 的纯色圆点。读屏用户本来就能听到名称，**明眼色盲用户**（约 8% 男性）看到的却是四个几乎一样的灰点，只能一个个点开试。
  - 现在色块上直接印着「重点 / 疑问 / 待查 / 喜欢」，颜色照旧保留；选中的那颗会同时**加粗**，不再只靠一圈边框颜色来区分。
- **删除确认框里按 Tab 不再"跑丢"**
  - 确认框标着 `aria-modal`，但之前只做了"打开时把焦点放到「取消」上"——按一下 Tab 焦点就跑到背景书卡上去了，键盘和读屏用户会在看不见的弹层与背景内容之间迷路。
  - 现在 Tab / Shift+Tab 在「取消 / 移除」之间循环；关掉确认框后焦点回到原来那个「×」，不用从页首重新 Tab 一遍。

### 新增（排版：恢复默认 + 主题跟随系统）

- **排版面板新增「恢复默认排版」**
  - 滑到一半想回头，以前只能记住默认值一个个手调回来。现在面板最底下一个按钮全部还原（字号 18px、行距 1.9、页边距 20px、字体与主题回到初始）；**上传的自定义字体不会被清掉**。
- **主题新增「跟随系统」，而且它就是默认值**
  - 新用户不必先找到「夜间」按钮：系统是深色模式时进阅读页直接就是夜间，系统日夜切换时页面跟着变（不用刷新）。
  - 手点过「日间 / 护眼 / 夜间」任意一个即视为明确选择，从此**固定下来**，不再被系统偏好改掉；想回到跟随系统，点回「跟随系统」即可。
  - 选「跟随系统」时面板会注明当前实际显示成哪一套（例如「正在跟随系统，当前显示为『夜间』」）。

### 修复（导入：安卓夸克等国产内核上选不了书）

- **在安卓夸克这类浏览器上，「导入书籍」点了没反应、或者选不了文件**
  - 选文件这一步用了三处在国产内核（UC / 夸克 / U4）上会失效的写法，现在都改掉了：
    - **`accept` 原先写了 MIME**（`application/epub+zip`）。安卓文件管理器对 `.epub` 报的类型常常是 `application/octet-stream`（系统压根没注册 epub），国产内核比对不上，就会把书在列表里**置灰不可选** —— 表现为"弹出了选择器，但书就是选不了"。现在只写扩展名 `.epub,.txt`。
    - **隐藏文件的 `<input>` 原先用 `display: none`**。部分内核不响应不可见元素的点击，选择器干脆不弹 —— 表现为"点了导入毫无反应"。现在改成移出视口 + 透明。
    - 补上"取消"的收尾：在系统选择器里点取消也能正常结束，不再每次取消都在页面上留一个看不见的元素。
  - 三条都写成了回归测试，**改回去会被当场拦下**。

### 修复（栏宽：留白 0 现在真的铺满）

- **「页边距」拖到 0，两侧不再留着大片空白**
  - 罪魁不是滑块，而是正文容器的**栏宽上限恒为 760px、与滑块毫无关系**：窗口一宽，760px 居中之外的两侧空白就固定在那儿，留白调到 0 也一分不少（1512 视口实测**两侧各空 206px**）。
  - 现在栏宽上限跟着滑块走：留白 ≥ 20px（默认值）时仍是标准 760px 栏宽、**观感不变**；往 0 拖时上限线性放宽，**0 就是铺满可用宽度**。
  - 手机等窄屏（可用宽 < 760px）本来就不受上限约束，行为完全不变。
  - 排版面板的说明补了一句「拖到 0 正文铺满」。

### 修复（跨设备：手机上留白不再把正文压成一条线）

- **手机把「页边距」拖到底，正文不再被挤成一条缝**
  - 留白是**绝对像素**（0–120）：同一个值在 1440px 屏上只占 8.3%，在 320px 屏上却占 37.5%。实测 320px 屏留白 120 → **正文只剩 52px（一行 2 个字）**。
  - 现在留白按屏宽封顶，**每侧不超过屏宽的 12%**：320px 屏最多 36px、390px 屏最多 44px；而 1440px 及以上依旧是原来的 120px —— **桌面观感完全不变**。
  - 滑块的**上限也跟着屏宽收**：手机上不再有"右半段轨道怎么拖都没反应"的死区（屏幕只认 44px，却让你拖到 120），转屏后上限自动放开。排版面板会注明本屏上限，例如「本屏每侧最多 44px：再往里挤就放不下一行了」。
- **长网址、长数字串不再顶破页面**
  - 正文里不可断的长串（URL、连续数字）在窄栏里会**顶破容器**，逼出横向滚动条；而正文区禁止手指横向拖动，手机上溢出的字就成了"看得见、摸不着"。
  - 现在长串放不下时会自动断行，中文与正常英文段落不受影响。
- **手机横屏不再突然退回桌面布局**
  - 断点原先只看宽度（≤720px），而手机横屏是 844×390 —— 竖着看是手机布局，**一横过来就变回桌面侧栏、顶栏也不再横滑**，转个屏界面突变。
  - 现在断点补上"矮屏"这条（高度 ≤500px 且宽度 ≤1024px），手机横屏同样走移动布局。

### 修复（本轮：书签与剩余时间的可发现性）

- **顶栏「＋书签」改名「＋ 标记」——两个按钮都叫"书签"，谁也分不清**
  - 上一版顶栏并排着两个按钮：`＋书签`（加/取消当前位置）和 `书签 3`（打开书签列表）。**同名、相邻、功能完全不同**，结果就是"只能加书签，却不知道上哪看"。
  - 现在动作归动作、名词归名词：左边是 **「＋ 标记」/「已标记」**（加与取消），右边仍是 **「书签 N」**（查看列表）。
  - 加完之后的提示也一并改了：从干巴巴一句「已添加书签」改成 **「已添加书签 · 点顶栏「书签」可查看」**，直接告诉你下一步点哪儿。
- **书签在正文里终于看得见了**
  - 原先书签只存在于侧栏列表里，**正文毫无痕迹**——标完往下滚就找不到自己标了哪儿，很容易以为"点了没生效"。
  - 现在书签所在的段落左侧会出现一条竖线（用当前主题的强调色），取消书签即消失。
- **本章剩余时间改为常驻显示，不再要求你去点它**
  - 上一版必须点击顶栏那串百分比才能切出剩余时间，而它长得和纯文本一模一样（当时特意去掉了按钮的边框与底色）——**几乎没人发现它能点**。
  - 现在直接并排显示：**`62.3% · 剩约 12 分`**。拖动进度条时只显示拖动值（那会儿的剩余时间算出来是错的）。
  - 仍然保持保守：样本不足（读得不足 1 分钟或不足 2000 字）时**只显示百分比**，不编数字、也不显示"估算中"这种半截话占地方。

## [0.1.5] — 2026-09-14

### 新增（本轮：第三方评审跟进）

- **为本地数据申请「持久化存储」保护，并在拿不到时如实告知**
  - 浏览器默认把本地数据当成"随时可以丢"的：磁盘吃紧、长期不访问或清理缓存时，可能把整个站点的数据一起清掉——**书、进度、书签、全部高亮笔记、阅读统计都在里面**。之前我们没有申请任何保护，也没有告诉用户。
  - 现在启动时会向浏览器申请持久化存储（拿到后只有你手动清理才会消失）。若浏览器拒绝，**且书架上确实有书**时，书库会出现一行提示，说明数据可能被清理，并告诉你去阅读页用「笔记 → 导出」做备份；这行提示可以点「不再提示」永久关掉。
  - 拿不到保护时的设计刻意克制：空书架不提示（没东西可丢）、可以永久关闭——因为浏览器对全新访客本来就常常拒绝，逢人就弹只会让所有提示失效。

### 新增（本轮：阅读体验补齐 P1）

- **「笔记」入口不再在"一条笔记都没有"时被禁用**（P1-5）
  - 之前零高亮时这个按钮是灰的，而新用户从没用过、也就永远看不到这个功能存在——功能不可发现。
  - 现在始终可点开；面板空态从干巴巴的「还没有高亮」改成一句操作引导（在正文里选中一段文字，点浮层里的「加高亮」就有了）。
- **一键书签：顶栏单击即可添加 / 取消当前位置的书签，并可 `Ctrl+B`**（P1-3）
  - 原先必须「打开书签面板 → 点添加当前位置」两步，入口太深，也没有快捷键。主流阅读器都是工具栏一键（Apple Books 工具栏图标、Kindle 点右上角、Thorium `Ctrl+B`）。
  - 现在顶栏有独立的「＋书签」按钮：单击把当前视口位置存为书签，同一位置再点一次即取消，按钮会同步切成激活态（`aria-pressed`）；`Ctrl+B`（macOS 上 `Cmd+B`）与按钮同一动作，面板开着时也可用。
- **补齐标准快捷键：`Ctrl+F` 搜索、`Ctrl+Home`/`Ctrl+End` 书首书尾、`Ctrl+PageUp`/`Ctrl+PageDown` 上/下一章**（P1-4）
  - 原先这几类键全部无反应（实测：按 `Ctrl+F` 后搜索面板没打开，按 `Ctrl+End` 后 `scrollTop` 仍是 0）。
  - 现在按主流阅读器（Thorium）的约定补齐五个；切章会夹回正文区间，不会把你丢在封面 / 目录 / nav 这类边缘页上。
  - 顺带修掉一个隐患：焦点在搜索框 / 笔记框里时，方向键与 `Home`/`End` 让路给输入框自己的光标语义，不再把正文拽走。
- **可拖的书级进度条 +「上一章 / 下一章」**（P1-1）
  - 原先全屏唯一的位置反馈是顶栏一个百分比，厚书想跳到某处只能一路滚——我们已有的样本里就有单章 25 万字的书。
  - 现在顶栏下方有一条按**章**折算的进度条：拖到 x% 即跳到对应章节，两端是「上一章 / 下一章」（也支持 `Ctrl+PageUp`/`Ctrl+PageDown`）。
  - 两个刻意为之的细节：① **拖动中只更新数字与滑条，松手才真正跳章**——每移动一格都跳，在厚书上会疯狂加载章节；② 不拿原生滚动条凑数——它只覆盖"已加载的章节"，拖到 100% 触发的是加载下一章，看起来像进度倒退。
  - 进度条与顶栏同属"外壳"，手机上点正文中间收起顶栏时会一并隐藏，不白占一行。
- **本章剩余时间：点击顶栏百分比即可查看**（P1-2）
  - 原先只有百分比，"这一章还有多长"没有答案。现在复用已有的累计阅读时长与当前进度倒推阅读速度（字/分钟），再乘"本章还剩多少字"，给出「剩约 X 分」；**点击顶栏百分比**即可在「百分比 / 剩余时间」之间循环（照搬 Kindle 的交互）。
  - 刻意保守：读得不足 1 分钟或不足 2000 字时**不给数字**（显示"剩余时间估算中"）；速度夹在 200–2000 字/分钟之间，防止"刚打开就拖到 90%"这类操作倒推出荒谬值；权重为 0 的脏 EPUB 一律退回百分比。
- **书库支持排序与筛选**（P1-6）
  - 原先只有"导入时间倒序"一种排法，也没有筛选；书一多（>20 本）就找不到想读的那本。
  - 新增排序下拉（**导入时间 / 最近阅读 / 书名 / 作者**）与书名、作者即时筛选框。「最近阅读」会把从未读过的书排在最后。
  - 筛选没结果时给的是「没有匹配的书 · 换个关键词」，而不是误报「书架是空的」。
- **高亮支持四种颜色：重点 / 疑问 / 待查 / 喜欢**（P1-7）
  - 原先所有高亮都是同一种黄色，无法区分"这段要紧"和"这段没看懂"。
  - 现在确认浮层里直接选色（黄·红·蓝·绿），新划的高亮按所选颜色落库；点已有高亮时也能就地改色，改色**只动颜色、不动锚点与笔记**。
  - 关键设计：库里存的是**颜色键**而不是具体色值，实际色值由 CSS 按当前主题给出——否则夜间模式下"浅黄底 + 浅色字"会糊成一片。老数据里存的旧色值仍按原样内联渲染，存量高亮的样子一点不变。

## [0.1.4] — 2026-09-12

### 修复（本轮：安卓实测反馈）

- **新增「系统默认」字体，并成为默认值——移动端终于有能用的字体了**
  - 内置的宋体 / 楷体 / 圆体 / 仿宋是桌面字体，**iOS 和安卓都没有这些字体文件**，安卓上 5 个内置选项会全部被判为不可用，用户面对一排灰按钮，以为整个字体功能坏了。
  - 现在多了一个「系统默认」（不指定字体，交给各平台自己的中文字体），**在四个平台上都真实生效**，并且成为默认值。已存的用户设置不受影响，不会被擅自改动。
- **字体栈补进各移动平台真实存在的中文字体名**
  - 之前栈里只写了 macOS / Windows 的字体，安卓的实际默认中文字体 `Noto Sans CJK SC` 从来没写过 —— 于是"本可用"的黑体也被探测判成不可用。
  - 现在补齐：安卓 / 原生（`Noto Sans CJK SC` 及衬线版 `Noto Serif CJK SC`）、华为（`HarmonyOS Sans SC`）、小米（`MiSans`）、开源思源（`Source Han Sans/Serif SC`）、开源楷体（`LXGW WenKai`）。安卓上「黑体」因此变得可用。
- **探测自身加了自检，避免"所有字体全被标灰"**
  - 部分浏览器（某些安卓浏览器 / 隐私模式）会把 canvas 字体度量抹平，导致每个字体都被判成"不可用"——正是"5 个全灰"的另一种成因。
  - 现在先自检探针是否可信，校验两个方向：不存在的字体名必须被判为不可用；三个通用族之间必须能量出宽度差。任一方向不成立就认为探针不可信，此时**一律当可用**——宁可让用户点到一个没效果的字体，也不要把整个字体功能显示成坏的。
- **字体说明从三行文字压成一行，且不再写死平台名**
  - 原先每个不可用字体都挂一个「不可用」角标，下面还有三行说明 + 一行红字，390px 的手机屏被文字占满；说明里还写死了"iPhone 的 Safari"，在安卓机器上照样显示，既占地方又不对劲。
  - 现在：不可用的字体只用淡化表达（禁用态是通用视觉语言），说明收进**一行可展开的摘要**（如"4 种字体在本机不可用"），文案改为中性的"本机没有这些字体文件"，**不出现任何平台名**。

### 测试（本轮：安卓实测反馈）

- `tests/fontAvailability.test.ts` 新增 8 例：候选表为空＝永远可用（「系统默认」走这条）、探针自检的两个方向（度量被抹平 / 把不存在的字体量出差异都判为不可信）、探针不可信时全部当可用而不是全部标灰，以及安卓场景回归（黑体命中 `Noto Sans CJK SC` 判可用、宋体楷体如实判不可用）。
- `tests/settings.test.ts` 新增 7 例：`system` 栈与默认值、字体栈含各移动平台字体名、`system` 不被归一化吃掉、老用户的显式选择不被改动、**每个探测候选都必须真的写在字体栈里**（否则会探测出一个栈里根本没有的字体）。
- `tests/app-ui.test.tsx` 更新：字体面板改为断言禁用态与折叠摘要（无「不可用」角标、摘要默认收起、文案里不含平台名）。

### 修复（本轮：iPhone 实测第二轮）

- **「页边距」滑块现在真的能用了（手机上尤其明显）**
  - 它原来调的是正文的**最大宽度**，取值区间 480–900px。可手机视口才 390px，最小值都已经超出屏宽 —— 于是滑块从这头拖到那头，正文宽度纹丝不动，看着就是个坏掉的控件。
  - 现在改成调**左右留白**（0–120px）：宽屏下在栏宽上限（760px）之内继续收窄，窄屏下才是真正决定"一行几个字"的那个量。手机实测：留白 0 → 每行 362px，40 → 282px，120 → 122px，全程可见地生效。
  - 标签也从「页边距（正文宽度）」改成「页边距」，并补一句说明"往右拖两侧留白变宽、每行字数变少"。
  - 旧版存下来的值（480–900）语义相反，加载时一律回落默认，避免把 680 当成 680px 留白、正文被挤成一条线。
- **夜间模式下侧栏、面板、批注浮层不再"刺眼纯白"**
  - 根因：主题 class 原先只重绑了正文那两个变量（`--reader-bg` / `--reader-fg`），而面板用的 `--surface` / `--fg` / `--muted` / `--line` 只在 `:root` 定义过 —— 于是夜间是"深色正文 + 纯白侧栏"，13px 的小字标签在强反差下尤其难读。
  - 现在每套主题都把整块调色板重绑一遍：夜间面板跟随转成深色（`#232328`），并提亮 `--accent` / `--danger`（原来的 `#534ab7` / `#a32d2d` 在深底上只有 2–3:1）；护眼面板转成暖米色。
  - 顺带修掉一个更隐蔽的：`.settings-header` 这类**没显式写 `color`** 的元素继承的是 `body` 上的旧值（CSS 变量在声明它的元素上就解析定型，不会因后代重定义而回溯），夜间会变成"深底深字"，实测对比度只有 1.12:1。现在在 `.reader` 上落一次 `color: var(--fg)` 兜住整棵子树。
  - 三套主题下 5 类文字元素实测对比度最低 5.52:1（护眼）/ 5.37:1（日间）/ 5.62:1（夜间），全部过 WCAG AA 4.5:1。

### 测试（本轮：iPhone 实测第二轮）

- `tests/settings.test.ts` 新增 5 例：页边距旧语义（480/680/900）一律回落默认、新语义范围内原样保留、越界与非法值收敛、新旧值域无交集、默认留白在手机与宽屏下都留得下正文。
- `tests/app-ui.test.tsx` 新增 2 例：页边距滑块调节的是"左右留白"且最小值小于手机视口宽（旧版恒被屏宽吃掉的回归）、切夜间主题时 `.reader` 挂上 `theme-night`。

### 修复（本轮：UX 审查 P0 止血批）

- **移除书籍不再是一键直删，改成「确认 + 8 秒可撤销」**
  - 之前点卡片右上角 × 会**立刻**删掉书 + 进度 + 书签 + 全部高亮与笔记 + 阅读统计，没有任何确认，误触即无法挽回（这是全项目唯一一处会丢数据的操作）。
  - 现在：点 × 弹出确认框，明确列出会一并删除的四类数据；确认后卡片先"乐观移除"并弹出「已移除《…》· 撤销」，**8 秒内点撤销可完全恢复**（IndexedDB 在这一步压根没写，撤销不是"再存回去"，是"取消删除"）。
  - 默认焦点给「取消」，按 Esc 等同取消；8 秒窗口到期或离开书库时才真正落库。宁可少删，不可错删。
- **补上 `↑` / `↓` 方向键翻屏，正文区可被键盘聚焦**
  - 之前键盘翻页只接了 `←` `→` `空格` `PageUp` `PageDown` `Home`，**漏掉上下方向键**——用户下意识按 `↓` 毫无反应，观感像"软件坏了"。
  - 现在 `↑`/`↓` 与 `←`/`→` 同义，步长一致（一屏高度 − 48px）。
  - 滚动容器补 `tabIndex=0` 与可见焦点环，之前 `tabIndex=-1` 导致正文区无法用 Tab 聚焦，键盘用户只能靠挂在 window 上的全局监听"蹭"到翻页。

### 测试

- `tests/app-ui.test.tsx` 新增 5 例：点 × 弹确认且书未被直删、取消关闭确认框、确认后进入撤销窗口且 IndexedDB 未动、撤销后书恢复、窗口到期才落库、`↑`/`↓` 触发翻屏且方向相反、`←`/`→` 步长一致、正文区 `tabIndex=0`。

### 修复（本轮：iPhone 实测反馈）

- **手机上侧栏不再遮满屏、也不再互相叠**
  - 原来「目录」「排版」两个按钮各管各的，能同时打开：280px + 300px = 580px 挤在 390px 的屏幕上，正文被压成一条缝、右边的排版还被裁掉一半。
  - 现在**一次只开一个面板**（点开一个会自动收起另一个）；手机上侧栏一律改成**从底部升起的浮层**（宽度占满、高度上限 78%、圆角 + 阴影），正文区不再被压窄；浮层上方带遮罩，点遮罩或 × 收起。桌面端仍是并排侧栏，行为不变。
- **字体不再"点了没反应却毫无解释"**
  - 部分移动端浏览器（iOS Safari 的字体指纹防护、安卓各家 ROM 的字体差异）会让某些字体名点了没反应——用户在设置里点半天看不到变化，只会以为软件坏了。
  - 新增运行时探测（`src/lib/fontAvailability.ts`，宽度比对法，两个回退字体交叉验证）：进阅读页时探一次内置字体在本机的真实可用性，不可用的禁用并给出可展开的说明，同时引导用「＋自定义」上传字体文件（走 FontFace 注册，各平台一致）。探测逻辑与文案在后续一轮按安卓实测反馈做过调整，见本轮条目。
  - 探测不出来（没有 canvas 的环境）时一律当可用——宁可点到没效果的字体，也不把选择全禁用。

### 测试（本轮：iPhone 实测反馈）

- 新增 `tests/fontAvailability.test.ts`（7 例）：字体存在/被屏蔽/宽度恰好与回退字体相同的边界、候选表顺序取第一个可用、探测不可用时一律返回可用。
- `tests/app-ui.test.tsx` 新增 4 例：开排版会自动关掉目录、再点同一个按钮收起、面板打开时渲染遮罩且点遮罩收起、不可用字体标灰并给出上传字体的说明。

### 新增（本轮：触屏手势，P0-3）

- **手机上能翻页了**
  - 点正文**左 / 右边缘**（各 22%）= 上一屏 / 下一屏；**左右滑动**同义（横距 ≥45px、纵向不主导、700ms 内）。两者都用平滑滚动，步长与键盘翻页一致。
  - 点正文**中间** = 收起 / 唤出顶栏，手机上多出一整行正文，沉浸式阅读。
  - 侧栏（目录 / 排版 / 书签 / 搜索 / 笔记）开着时，点正文 = 关掉侧栏，不翻页。
  - **只认触屏**：鼠标点击不参与——鼠标点正文有"选词 / 放光标"的语义，一并当翻页会废掉高亮功能。
  - 不会误伤：落在链接（脚注）、按钮、高亮、图片上的点按不翻页；长按选词时（有选区）不翻页。
- **手机适配**：正文区加 `touch-action: pan-y` + `overscroll-behavior-y: contain`（关掉下拉刷新、避免横向露白）；屏幕 ≤720px 时顶栏横向可滑、按钮触控区加大、正文左右留白收窄。
- **阅读页高度改用 `100dvh`**：iOS Safari / Chrome 的地址栏会伸缩，`100vh` 用的是"工具栏收起"的大视口高度，工具栏展开时正文最后一行会被底部工具栏遮住。不认 `dvh` 的老浏览器自动退回 `100vh`。

### 测试（本轮）

- 新增 `tests/gestures.test.ts`（13 例）：分区边界值、点按与滑动的判定阈值、纵向为主不抢滚动、超时不算翻页。
- `tests/app-ui.test.tsx` 新增 6 例：点左右翻屏、点中间收起/唤出顶栏、滑动翻屏且纵向滑动不抢、点链接不翻页、有选区不翻页、侧栏开着时点正文只关侧栏。

## [0.1.3] — 2026-09-11

### 新增

- **阅读时长统计（P1）**
  - 书库页每本书卡片新增一行「已读 X 小时 Y 分 · 读了 N 次」，记录累计阅读时长与打开次数。
  - 进入阅读页即开始计时：仅在前台（标签页可见）累计，切到后台 / 息屏自动暂停；每 30 秒与退出阅读时把已读时长持久化，不怕意外关闭。
  - 数据存 IndexedDB（新增 `stats:` key），与书、进度、书签同级；删书时连带清理，不留孤儿记录。

### 测试

- 新增 `tests/stats.test.ts`（存储层：touchOpen / addReadingSeconds / listStats / 删书级联清理，5 例）。
- `tests/app-ui.test.tsx` 新增集成用例：打开书进入阅读页会记录一次阅读会话。

### 新增（本轮：高亮与笔记 / 全文搜索）

- **字符级高亮与笔记（P1）**
  - 在阅读页**框选任意文字**即生成高亮（默认浅黄），点高亮弹出笔记浮层可写 / 改 / 删笔记。
  - 锚点用「章序号 + 块序号 + 块内字符偏移」精确还原，**不依赖一次性 DOM 包裹**——章节用 `dangerouslySetInnerHTML` 渲染，任何重渲染都会冲掉包裹，所以高亮靠「存锚点 + 加载后 effect 重绘」，重渲染幂等不丢。
  - 与进度锚点共用同一套 `BLOCK_SELECTOR`，块索引严格对齐。
  - 数据存 IndexedDB（新增 `annotations:` key），删书级联清理。
- **单书全文搜索（P1）**
  - 顶部「搜索」打开面板，输入关键词实时检索**本书**正文（主上大人拍板：仅单书范围），命中按章节列出、关键词高亮，点击跳到对应段落。
  - 抽取复用 `weights.ts` 的 zip 直读思路，文本只抽一次缓存到内存。
- **笔记导出（纯本地，可选中导出）**
  - 顶部「导出」打开选择面板：列出本书全部高亮（含章节、原文摘要、笔记内容），**默认全选、可逐条勾选**，点「导出选中」才生成 Markdown 下载（回答了"到底导出哪些"）。
  - 导出后右下角弹出明确提示「已导出 N 条高亮（含 M 条笔记）」，不再静默下载。
  - 不联网、不跨设备（PRD 把云同步 / 跨设备砍了，本地备份用导出兜底）。

### 修复（本轮：高亮与笔记实测回归）

- **高亮不再波及不相关的段落（定位范围错乱）**
  - 根因：创建选区时用 `range.toString().length` 算字符偏移，而重绘还原时用「文本节点 `.data.length` 累加」——**两套口径不一致**（`<br>` 会被 range.toString 渲染成 `\n`、空白被折叠），导致偏移错位、`wrapRange` 越界跨块，把其他段落也一并高亮。
  - 修复：创建与重绘**统一为同一套计数**（文本节点累加）；`locateInBlock` / `selectionToAnchor` / `applyHighlights` 全部加 clamp，`wrapRange` 强制校验两个端点都在目标块内，越界即跳过，从机制上杜绝跨块包裹。
- **笔记浮层可以正常关闭，且关闭不会带走其他笔记**
  - 原来只有浮层内的「关闭」按钮能关，点正文别处浮层一直挂着，观感像"关不掉"。现在**点击浮层外部任意位置即可关闭**。
  - 原先"关闭后其他笔记也消失"是上一条重绘错乱的连锁反应：偏移越界会让部分高亮重绘失败而视觉消失（数据其实仍在库里）。偏移口径修好后一并解决。
- **导出有反馈、可选范围**（见上方"笔记导出"）。

### 修复（本轮：笔记功能第二轮实测回归）

- **框选不再"自动变成笔记"，选错可以取消**
  - 原来一框选就立刻写进 IndexedDB，框错一段（或只是想选中复制）也会留下一条高亮，只能事后去面板里删。
  - 现在框选只弹**确认浮层**（显示摘录 + 可选笔记），点「加高亮」才入库；点「取消」/ 浮层外 / `Esc` 一律放弃，什么都不会留下。
  - 与已有高亮完全重叠时提示「这段已经高亮过了」，不重复创建。
  - `Esc` 的语义也修了：有浮层或面板开着时先关它们，而不是一按就把整本书关掉。
- **多次高亮后，之前的高亮不再"消失"**
  - 两条根因：① `applyHighlights` 是先 `unwrapAll` 再逐条包裹，中途任何一段 `extractContents` 抛错（脏 HTML、异常嵌套）就会中断——**旧高亮已被拆掉、新的没画上，整章高亮全空**；② 重叠区间是"跳过"策略，先划小段、再划大段把它包住时，大段先画、小段因起点落在已覆盖区被丢弃，看着像"之前那条没了"。
  - 修复：每段包裹独立 try/catch、每块独立 try/catch，单段失败只丢那一段；重叠区间改为**合并**成一段连续高亮，被合并的 id 全部记在 `data-ann-ids` 上，导出/删除仍按原始多条处理，数据一条不少。
- **笔记可以管理：导出面板升级为「高亮与笔记管理」**
  - 顶部按钮由「导出」改为「笔记 N」，面板改名「高亮与笔记管理」。
  - 每条右侧新增**「删除」**按钮，可就地删单条；底部新增**「删除选中」**支持批量删。
  - 删除会同步做四件事：库里删干净、列表刷新、勾选态清理、浮层关闭，不留残留。

### 修复（本轮：高亮滚动即消失 + 笔记可跳转）

- **高亮不会再"一滚动就没了"**
  - 根因（真实浏览器复现确认）：滚动会更新进度 → React 重渲染 → 章节是用 `dangerouslySetInnerHTML` 灌进去的，重渲染时整段 innerHTML 被重写，画好的 `<mark>` 被冲掉；而负责重绘的 effect 依赖 `[loaded, annotations]`，滚动时这两个依赖都没变，**effect 不会重跑**，高亮就再也回不来——只有下次增删高亮（annotations 变化）才会被重新画出来，正是"再创建一次，之前的又出现了"。
  - 修复（双管齐下）：① **章节正文 memo 化**（`ChapterBody`）——html/css 引用不变时 React 跳过这个子树，从源头不再重写 DOM；② **高亮守卫**——每次渲染后比对「这一章理应有多少 `<mark>`（`countSegments`）vs 实际数量」，对不上就补画，扛住任意次数的重渲染。
  - 真机验证：创建高亮后连续滚动 4 次，`<mark>` 始终存在（修复前第 2 次滚动即归零）。
- **笔记管理面板：点条目直接跳到书中对应位置**
  - 点笔记条目（章节 + 原文 + 笔记那一块）即关闭面板并滚到正文该段落；目标章节若未加载会自动加载，跳过去后补一次高亮重绘，确保能看到黄的那一段。
  - 跳转沿用书签那套重试节奏（懒加载图片会陆续把布局顶动，只滚一次不准）。

### 测试

- 新增 `tests/annotations.test.ts`（存储层 CRUD / 去重 / 导出 / 删书级联，5 例）。
- 新增 `tests/search.test.ts`（htmlToText 清洗 + searchChapters 命中，5 例）。
- 新增 `tests/highlight.test.ts`（选区→锚点 / applyHighlights 包裹 / 幂等 / 多段 / **多块隔离** / **重叠合并** / 越界 clamp / unwrapAll，9 例）。
- `tests/app-ui.test.tsx` 新增集成用例：框选后需确认才入库、取消不留痕、管理面板逐条删除、**反复滚动后高亮仍在**、**点笔记条目跳转**、搜索本书返回命中（36 例）。

## [0.1.2] — 2026-09-10

### 修复

- **排版设置被 EPUB 自带样式压过**（样本：《涛动周期论》）

  该书每个段落都焊死了 `style="font-size:16px;font-family:'PingFang SC'"`。内联样式的优先级高于阅读器的 `.chapter` 规则，导致调字号没反应、切字体无变化、行距怎么调都不对劲。

  现在清洗阶段会摘掉内联的排版声明（`font-size` / `font-family` / `line-height` / `letter-spacing` / `color`），并用 CSS `!important` 兜住 EPUB 外部样式表里的同类规则；标题改用 `em` 相对值，层级保留且跟随字号设置一起缩放。语义样式（粗体、斜体、对齐）不受影响。

- **夜间模式下正文可能变成黑底黑字**

  同一根因：EPUB 内联的 `color:rgb(0,0,0)` 会盖过主题配色。现在文字颜色统一交给阅读器接管（链接保留强调色）。

- **部分书读不到封面 / 封面是错的**（样本：《博弈与社会》）

  这类书没有 cover 命名的封面页（页面全叫 `part0000.xhtml`），封面改由 OPF 的 `<meta name="cover">` 声明，且那张图不被任何页面引用——解析库只把章节引用到的资源转成地址，孤立资源拿不到，旧兜底链便直接放弃了。

  第一版修复用"书名页抠图"顶上，结果二次打脸：书名页里那张图是白底题名图（一行作者名），并不是真封面。

  现在按**访达/Quick Look 同款规则**取封面：优先认 OPF 的权威声明（`<meta name="cover">` 或 `properties="cover-image"`），直接从 EPUB 内部解出那张图（data URL 存储，刷新不失效）；封面页抠图降为第二顺位，书名页抠图最后保底。两本真实样本都验证了取到的图与 OPF 声明图**逐字节一致**，不再是"每来一本新书补一条规则"。

  顺带加了**封面版本号**：封面算法以后再修正，老书的封面会自动重取，不需要手动删书重导。

- **正文图片全部不显示**（样本：《涛动周期论》46 张图）

  根因在解析库 `@lingo-reader/epub-parser` 0.4.6：它用**模块级全局** `imageRecord` 缓存图片字节、`browserUrlCache` 缓存 blob URL，而 `destroy()` 会 `unlink` 全部条目 + `revokeBlobUrls()` 清空全局。本项目 `openEpub` 会被多次调用（导入 / 进阅读页 / 后台补封面），任一次 `destroy` 都清空全局状态，已渲染的 `<img src="blob:…">` 当场变成 0 字节空 blob（真浏览器实测 `naturalWidth=0`、fetch 回来 `byteLength=0`）。Node 测试走真实文件系统，这类"全局状态被跨实例互踩"的问题永远测不出。

  改为**自建资源层**（`src/lib/resources.ts`）：直接解 EPUB 的 zip 字节，把章节里的 `<img src>`、SVG `<image href>`、外链 CSS 的 `url()` 换成我们自己控制的地址（浏览器用 blob、Node 退化成 data URL）。地址生命周期归每本书自己管，只有这本书 `destroy` 时才 `revoke`，跨实例互不干扰；与封面那条路同源。

- **部分 EPUB 无法导入**（样本：《巴菲特致股东的信（原书第4版）》）

  解析库 `parseGuide()` 见到 `<guide>` 里没有 `<reference>` 子元素会**直接抛错**，整本书打不开。但空的 `<guide></guide>` 是**完全合法的 EPUB 2 结构**（calibre 转换产物常见）。新增 `fixEpubBytes`：导入前解 zip 检测并剔除空 `<guide>`，重新打包成新字节（level 0 不重压），绝大多数书零开销返回 `undefined` 用原文件。

- **翻页滚到底卡死、只能上翻**（样本：《笔记的方法》卡在 5.6%）

  旧实现两个缺陷叠加：① `loadChapter` 守卫 `|| loadingRef.current` 把忙时的并发请求**直接丢弃**（是丢弃不是排队）；② `IntersectionObserver` 只在交叉状态"变化"时回调，effect 依赖 `[loaded]` 每加载一章就 `disconnect` 再 `observe`，若哨兵仍在视口内（状态没变）浏览器不再补发回调。于是"请求被丢 + 之后再无回调" → 加载链永久断开，滚到底顶住；只有把哨兵滚出视口再滚回才有概率恢复。

  新方案：滚动位置是**连续可查量**，不依赖事件是否补发。`loadChapter` 改用 `inFlightRef`（按章节 index 记的集合）替代全局布尔量——同章只发一次请求，但不同章可排队；新增 `pump()` 从已加载末尾往后补相邻未加载章，每次加载完由 `[loaded]` effect 复查，天然自愈；`handleScroll` 触发 `pump`，移除 `IntersectionObserver` 与哨兵 div。

- **进度被存到末尾的目录页，再次打开"只有目录、翻不动"**（样本：《The Art of Focus》）

  这本转换版 EPUB 把**全书正文塞进单个 spine 项**，另外挂一个只有千把字的 `nav.xhtml` 目录页，且它排在 spine **最后**。旧逻辑把这类"边缘轻量章"当普通正文处理：滚到末尾时 `pump()` 会把目录页也加载进来当正文渲染（一长串链接），进度条随之顶到 100%，`progress.chapterIndex` 也被存成那个目录页的下标——下次打开就正好停在目录页上，而它是最后一项，于是"翻不动"。

  新增 `detectContentRange()`（`src/lib/progress.ts`）：按字数从 spine 两端各刮掉明显低于平均值（< 5%）的边缘章，得到真正的**正文区间**。三处据此收敛：① `pump()` 不再自动加载正文区间**之后**的章节（目录页不再被当正文渲染）；② 百分比只在正文区间内折算，落在区间之前算 0%、之后算 100%；③ 记进度与恢复进度都把章节下标夹回正文区间——即使读者滚过目录页，落盘的仍是最近的正文位置。首次打开仍从封面开始，不会一上来就跳过封面。

- **目录页整页是 `<div>` 时，滚动推不动加载链，后面几章永远加载不出来**（样本：《策略思维》）

  这本书的 `Contents` 页由 calibre 生成，147 个 `<div>` + 146 个 `<a>`，**一个块级元素（`p`/`h*`/`li`…）都没有**。而 `handleScroll` 原先把"补加载下一章"放在了 `if (blocks.length === 0) return` **之后**——于是当已加载的章都是这种无块级元素的前置页时，滚动事件进来先被提前返回挡掉，`pump()` 永远不被调用，加载链彻底停住：只显示封面 + 目录页，怎么滚都出不来正文（"打开只有目录页、无法再往下翻"）。

  修复：把 `pump()` 提到 `blocks` 判断**之前**——补加载与"能不能记进度"是两件事，前者不该依赖后者。进度计算仍需要块级元素，这条判断保留。

  真浏览器（Playwright）实测修复前后：《策略思维》修复前滚到底仍停在 `scrollHeight=5983` 的封面+目录；修复后同一操作加载出整本，`scrollHeight=333203`、正文可见。

- **点击正文里的脚注 / 目录链接不跳转，反而被弹回书库**（样本：《博弈与社会》等带书内锚点的书）

  正文是 `dangerouslySetInnerHTML` 渲染的，里面的 `<a href="part0004.xhtml#a005">` 走的是浏览器**默认跳转**；而本项目路由用的是 **HashRouter**——hash 就是路由本身。默认跳转把 `location.hash` 从 `#/read/xxx` 改成 `#a005`，`parseHash` 认不出阅读路由，`App` 当场渲染书库：读者点一下脚注就被弹出去了。

  两处修复：

  ① `Reader` 在内容容器上做事件委托（`handleContentClick`），拦下所有 `<a>` 点击——书内链接交给 `OpenedBook.resolveHrefToChapter()` 解析成「章序号 + 章内锚点」后跳转（跨章会先加载目标章），外链新标签页打开；**任何情况下都 `preventDefault`**，绝不让浏览器碰 hash。

  ② 新增 `resolveHrefToChapter()`（`src/lib/epub.ts`）：正文链接是"相对**当前章文件**"的相对路径（`part0004.xhtml`），而 spine 记的是"相对 OPF"的路径（`Text/part0004.xhtml`），解析库的 `resolveHref` 对这类链接**一律返回 `undefined`**（连它文档里的 `epub:` 前缀写法也不认，实测），所以自己归一化匹配——按当前章目录拼路径、解析 `.` / `..`、并收 basename 与全小写兜底键。

  真浏览器（Playwright）实测：导入《博弈与社会》，点击目录页里的 `<a href="part0004.xhtml#a005">`，hash 保持 `#/read/…` 不变、仍停在阅读页且滚动到了目标位置（修复前会被踢回书库）。

### 新增

- **自定义字体上传**

  排版面板的字体区新增「＋自定义」按钮：选一个 `.ttf / .otf / .woff / .woff2`，阅读器用 FontFace API 把它注册进页面并立即生效，跨平台一致（也绕开各浏览器对系统字体的指纹收敛）。字体二进制存在独立的 IndexedDB key，不污染设置对象；应用启动时会自动把已上传的字体重新注册。已上传的字体在面板里可一键删除（删掉当前选中的会自动回落到宋体）。

- **内置字体精简为 5 种跨平台字体**

  去掉了 macOS 独占的冬青黑体、思源黑体（Windows / Linux 上点它们只会回落到系统默认，造成"切了没反应"的错觉）。保留的宋体 / 黑体 / 楷体 / 圆体 / 仿宋在 macOS 与 Windows 上都有对应字体，切换结果一致。想用冬青黑体 / 思源黑体的用户，走上面的"自定义字体"上传即可。

### 测试

- 新增 `tests/sanitize.test.ts`（内联排版剥离，11 例）。
- 新增第二本真实样本《博弈与社会》进入回归测试，专门守住"封面走 OPF meta 声明"这条路。
- 新增 `tests/settings.test.ts`（字体栈解析 + 归一化迁移，10 例）、`tests/customFont.test.ts`（字体增删查 + FontFace 注册，5 例）。
- 新增 `tests/resources.test.ts`（自建资源层：zip 解包、相对路径解析、魔数兜底 MIME、图片/SVG/CSS 内联替换，覆盖 Node 退化 data URL 路径）。
- 新增 `tests/guide.test.ts`（空 `<guide></guide>` 与自闭合 `<guide/>` 被 `fixEpubBytes` 剥离、带内容的 guide 与无 guide 返回 `undefined`）。
- `tests/epub.test.ts`、`tests/real-book.test.ts` 图片断言改为校验 `blob:` / `data:` 前缀（不再误判为 `EPUB/` 开头）。
- `tests/app-ui.test.tsx` 阅读器用例里 `findByText('c1 的正文')` 等改为 `waitFor(() => expect(screen.getByText(...)).toBeInTheDocument())`。原因：`findByText` 底层 `getBy` 在 jsdom 下首检拿不到元素时直接返回 `null` 而不重试，openEpub 异步渲染与查询首检偶发竞态会导致整批测试不稳定；`expect(...).toBeInTheDocument()` 找不到时会抛错，交给 `waitFor` 重试即可稳过。
- 新增 `detectContentRange` 与"带正文区间"的 `computeWeightedPercent` 单测（`tests/logic.test.ts`），断言用两本真书的实际权重分布，锁定"目录页划出正文区间"的切分规则。
- `tests/app-ui.test.tsx` 增加"脏书"回归用例：进度落在末尾 nav 上时，打开的是正文而不是目录；正文区间之后的 nav 页不会被自动加载；另有"首章整页只有 `<div>/<a>` 时滚动仍能推动加载下一章"（用 `Element.prototype` 临时给滚动容器真实尺寸，否则 jsdom 下 `scrollHeight` 恒为 0，测不出这条路）。
- 新增第三、四本真实样本《The Art of Focus》《策略思维》进入 `tests/real-book.test.ts`，守住"轻量目录页划出正文区间"这条路（与既有样本一样，缺书自动跳过）。
- `tests/app-ui.test.tsx` 新增"脚注书"回归用例：点书内锚点链接时 `fireEvent.click` 必须返回 `false`（即 `preventDefault` 被调用）且 hash 不被改；点跨章锚点时目标章会被加载（用 `Element.prototype` 临时给滚动容器真实尺寸，避免 jsdom 下 `scrollHeight=0` 让 `pump` 提前把目标章加载完，从而测不出"点击才触发"）。
  - ⚠️ 踩坑记录：**别用 `findByText` 拿元素再点击** —— 它在 React 异步重渲染的间隙会返回**已脱离文档**的 stale 引用（实测 `document.body.contains(el) === false`），游离元素的事件冒泡不到 React root，`onClick` 根本不触发，会得出"功能没实现"的假结论。用 `container.querySelector(...)` 取当前 DOM 里的元素。
- `tests/real-book.test.ts` 的《博弈与社会》组新增"书内跨章链接能解析成「章序号 + 锚点」"用例，用真实脏书的 href 写法（相对当前章、带 `../`、纯 `#` 锚点、解析不到的目标）守住解析规则。

### 文档

- 新增 `docs/字体验证清单.md`：自定义字体功能的跨平台 / 多格式验证矩阵（Mac + Windows × ttf/otf/woff/woff2），已验证项打勾、未验证留空，供主上大人有条件时逐步回填；含通用通过标准与各形态（Mac Web / Mac 桌面 / Windows Web / Windows 桌面）的验证步骤。

## [0.1.1]

- 重新设计并替换 macOS 应用图标。
- 修复打包文件名版本号不跟随 git tag 的问题（改为 CI 用 tag 覆盖 `conf.version`）。
- README 补充未签名 App 的 `xattr` 兜底放行说明。

## [0.1.0]

- 首个可用版本：导入 EPUB/TXT、书库与进度、阅读视图与目录跳转、排版面板（字号/行距/页边距/字体/三主题）、进度记忆与继续阅读、键鼠翻页。
- macOS 桌面版（Tauri，未签名）与网页版（GitHub Pages）双通道。
