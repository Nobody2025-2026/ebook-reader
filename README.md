# 自制阅读器（ebook-reader）

一个让人能舒服地读完一本 EPUB 的本地阅读器。**纯前端、零后端、数据全存你浏览器里**，拖进去就能读，不需要注册、不需要联网。

> 网页版：打开链接即用，无需安装。桌面版：已打包成独立 macOS App，去 [Releases](https://github.com/Nobody2025-2026/ebook-reader/releases) 下载。

## 📦 下载：网页版 or 桌面版

本项目提供两种使用方式，按你喜好任选，**两者数据互不相通、各自独立**：

| 方式 | 适合谁 | 怎么用 |
|---|---|---|
| **网页版** | 不想安装、随手就读 | 直接打开 GitHub Pages 链接：[https://nobody2025-2026.github.io/ebook-reader/](https://nobody2025-2026.github.io/ebook-reader/) |
| **桌面版** | 想要独立 App、离线常驻 | 去 [Releases](https://github.com/Nobody2025-2026/ebook-reader/releases) 下载 `.dmg`，拖进「应用程序」文件夹即可 |

> ⚠️ 网页版与桌面版是两个独立的本地书库（浏览器的 IndexedDB 与 App 的 WebView 存储各自隔离），在其中一边导入的书不会自动出现在另一边。

### 桌面版关于「未签名」的说明

桌面版目前**未做 Apple 开发者签名与公证**（个人项目，避免年费与复杂流程）。首次打开时 macOS 会弹出「无法验证开发者」的 Gatekeeper 提示，这属于正常现象，按以下方式打开即可：

1. 在「访达」中**右键（或 Control + 点击）**该 App → 选择「打开」；
2. 在弹出的确认框中再次点击「打开」；
3. 之后即可正常启动。

也可在「系统设置 → 隐私与安全性」中，看到被拦截提示后点「仍要打开」。

如果以上两种方式都找不到「仍要打开」按钮，可在终端执行（需 App 已拖入「应用程序」文件夹）：

```bash
xattr -cr /Applications/ebook-reader.app
```

> 与网页版一样：书、进度、封面全部存在你本机，不上传任何服务器。

## ✨ 功能

- **导入书籍**：拖拽或文件选择，支持 EPUB（TXT 顺带）
- **书库页**：封面网格、书名作者、阅读进度百分比、自动补封面；删书需两步确认（会明确列出将一并删除的进度 / 书签 / 笔记 / 统计），确认后仍有约 8 秒可撤销
- **阅读视图**：正文渲染、图片内联、连续滚动
- **目录导航**：侧边目录树，点击跳转章节（含章内锚点跳转）
- **正文内链接**：脚注 / 交叉引用点击即跳（同章滚动、跨章自动加载目标章），不会被浏览器默认跳转踢回书库
- **排版调节**：字号、行距、页边距、6 种内置字体（系统默认 / 宋体 / 黑体 / 楷体 / 圆体 / 仿宋）、四选主题（跟随系统 / 日间 / 护眼 / 夜间，默认「跟随系统」）
  - 页边距调的是**正文两侧留白**（0–120px），手机上同样生效；各套主题会一并接管侧栏与面板的配色，夜间不会留一块白面板
- **自定义字体**：可上传 `.ttf / .otf / .woff / .woff2`，跨平台一致生效
- ⚠️ **字体可用性说明**：宋体 / 楷体 / 圆体 / 仿宋是**桌面字体**，iOS 与安卓都没有这些字体文件，在那上面会被浏览器换成默认字体。设置面板会在进阅读页时探测一次，把本机真正不可用的字体禁用，并在下方给一行可展开的说明；**「系统默认」在任何平台都可用**（它交给系统自己的中文字体），也是新的默认值。想要确定的字形，请用「＋自定义」上传字体文件，走 FontFace 注册，各平台完全一致。
- **进度记忆**：按正文字数加权计算的阅读百分比，自动保存，重开回到原处
- **书签**：一键记录当前位置，列表可跳转、可删除
- **高亮与笔记**：选中正文先弹确认再落库（不会选完就自动建）；可给高亮写笔记，笔记列表点击可跳回原文，也能逐条删除
- **全文搜索**：单本书内搜正文，结果显示上下文，点击直达
- **阅读时长统计**：自动累计每本书的阅读时长（切到后台 / 息屏时暂停，不虚增）
- **笔记导出**：勾选高亮与笔记导出为 Markdown，纯本地下载、不联网
- **键盘操作**：`←`/`→`/`↑`/`↓` 翻屏、`空格` 翻屏、`PageUp`/`PageDown` 翻屏、`Home` 回本章开头；`Ctrl/⌘+B` 标记（取消）书签、`Ctrl/⌘+F` 搜索本书、`Ctrl/⌘+Home` / `Ctrl/⌘+End` 跳书首 / 书尾、`Ctrl/⌘+PageUp` / `Ctrl/⌘+PageDown` 上一章 / 下一章；`Esc` 关掉浮层与面板（都没开时回书库）。**阅读页顶栏最右的「?」（或直接按 `?`）可随时查看这份清单**，首次进阅读页还会提示一次
- **触屏操作**（手机 / 平板）：点左右边缘 = 上一屏 / 下一屏，左右滑动 = 翻屏，点正文中间 = 收起 / 唤出顶栏

## 🚀 快速开始

### 环境要求

- Node.js 18+（推荐 22）

### 本地运行

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

然后打开终端提示的地址（默认 `http://localhost:5173/`）。

> ⚠️ 数据按「域名 + 端口」隔离存储。换端口 = 换了一个空书库。固定用同一个端口，书和进度才一直在。

### 构建

```bash
npm run build      # 类型检查 + 产出 dist/
npm run preview    # 本地预览构建产物
```

### 测试

```bash
npm test           # 跑完整测试套件
npm run test:watch # 监听模式
```

## 🧱 技术栈

| 项 | 选择 |
|---|---|
| 框架 | Vite + React 19 + TypeScript |
| EPUB 解析 | `@lingo-reader/epub-parser` |
| 存储 | IndexedDB（`idb-keyval`）：书文件 + 进度 + 书签 + 高亮笔记 + 阅读统计 |
| 状态管理 | React 内置（`useState` / `useRef`），未引入状态库 |
| 测试 | Vitest + Testing Library |
| 解压 | fflate（字数加权进度用）|

## 📁 项目结构

```
src/
├── components/   # Library（书库）、Reader（阅读视图）
├── lib/          # epub 解析、封面提取、进度、排版、存储、路由等
├── App.tsx       # 顶层路由 + 书库/阅读切换
└── main.tsx      # 入口
docs/             # PRD 等文档
tests/            # 测试
```

## 🤝 使用与授权

- 本项目采用 [MIT 许可证](LICENSE)，可自由使用、修改、分发。
- 数据（书、进度、封面）全部存在你的浏览器本地 IndexedDB，不上传任何服务器。
- 启动时会向浏览器申请**持久化存储**，避免数据在磁盘紧张或清理缓存时被自动清除；若浏览器拒绝，书库会出现一行提示（可关闭），建议用「笔记 → 导出」备份重要笔记。

## 🗺️ 路线图

> **定位**：一个让人舒服地读完一本 EPUB 的本地阅读器。取舍标准只有一条——这个功能是否直接服务于「把书读完」？是则留，否则延后。

### ✅ 已发布（v0.1.0 – v0.1.6 · 2026-09）

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| **P0 · MVP** | 导入、书库与进度、连续滚动阅读、目录、排版（字号 / 行距 / 页边距 / 主题）、进度记忆、键盘快捷键 | 已发布 |
| **P1 · 体验增强** | 书签、高亮与笔记（四色）、单书全文搜索、阅读时长统计、笔记导出、快捷键速查、首次引导 | 已发布 |
| **分发 · 网页版** | GitHub Pages，打开即用，无需安装 | 已发布 |
| **分发 · 桌面版** | Tauri 打包的 macOS 独立 App（未签名，按上方说明放行） | 已发布 |

### 🔜 下一阶段（聚焦打磨，未排具体版本）

在已发布功能上做「减法式」打磨：稳定性、各端真机手感、阅读无障碍与读屏优化。

### 🔭 远期探索（未承诺排期）

AI 辅助阅读：划词翻译 / 解释、术语追问、智能笔记整理、单章 RAG 问答、全书摘要（方案见 `docs/ai-feature-plan.md`）。

### ⏸️ 暂缓（不在本期 MVP 范围）

账号与云同步 · 多设备同步 · PDF 内阅读 · TTS 朗读 · 字典翻译 · 书籍导出与分享

> 本期只聚焦把「舒服地读完一本 EPUB」这一件事做扎实，以上功能暂缓；**非永久不做**，待核心阅读体验稳定后再评估。
